───────────────────────────────────────────────────────────────
💾 DATABASE NAME
───────────────────────────────────────────────────────────────
ExpenseTracker
───────────────────────────────────────────────────────────────


🧱 TABLES OVERVIEW
───────────────────────────────────────────────────────────────
| No.| Table Name   | Purpose                                          |
| ---| ------------ | ------------------------------------------------ |
| 1️⃣ | Users              | Stores user registration and login details.                    |
| 2️⃣  | Categories       | Stores user expenses categorized by type.                    |
| 3️⃣  | Income            | Stores income entries and sources for each user.           |
| 4️⃣  | Budget            | Stores monthly budget goals and tracking info.              |
───────────────────────────────────────────────────────────────



───────────────────────────────────────────────────────────────
1️⃣ USERS TABLE
───────────────────────────────────────────────────────────────
Purpose: Stores registered user details and login credentials.
───────────────────────────────────────────────────────────────

CREATE TABLE [dbo].[Users] (
    [UserID]    INT IDENTITY (1, 1) NOT NULL,
    [Username]  NVARCHAR (100) NOT NULL,
    [Email]     NVARCHAR (100) NOT NULL,
    [Password]  NVARCHAR (100) NOT NULL,
    [CreatedAt] DATETIME DEFAULT (GETDATE()) NOT NULL,
    PRIMARY KEY CLUSTERED ([UserID] ASC)
);
───────────────────────────────────────────────────────────────



───────────────────────────────────────────────────────────────
2️⃣ CATEGORIES TABLE
───────────────────────────────────────────────────────────────
Purpose: Stores all user expenses under specific categories
         (like Food, Travel, etc.).
───────────────────────────────────────────────────────────────

CREATE TABLE [dbo].[Categories] (
    [CategoryID]   INT IDENTITY (1, 1) NOT NULL,
    [CategoryName] VARCHAR (100) NOT NULL,
    [Description]  VARCHAR (500) NULL,
    [Amount]       DECIMAL (18, 2) DEFAULT ((0.00)) NOT NULL,
    [UserID]       INT NOT NULL,
    [CreatedDate]  DATETIME DEFAULT (GETDATE()) NOT NULL,
    PRIMARY KEY CLUSTERED ([CategoryID] ASC),
    FOREIGN KEY ([UserID]) REFERENCES [dbo].[Users] ([UserID])
);
───────────────────────────────────────────────────────────────



───────────────────────────────────────────────────────────────
3️⃣ INCOME TABLE
───────────────────────────────────────────────────────────────
Purpose: Tracks income sources and amounts for each user.
───────────────────────────────────────────────────────────────

CREATE TABLE [dbo].[Income] (
    [IncomeID]     INT IDENTITY (1, 1) NOT NULL,
    [CategoryID]   INT NULL,
    [UserID]       INT NOT NULL,
    [Source]       NVARCHAR (100) NULL,
    [Amount]       DECIMAL (10, 2) NULL,
    [DateReceived] DATETIME DEFAULT (GETDATE()) NULL,
    [Description]  NVARCHAR (255) NULL,
    PRIMARY KEY CLUSTERED ([IncomeID] ASC),
    FOREIGN KEY ([CategoryID]) REFERENCES [dbo].[Categories] ([CategoryID]),
    FOREIGN KEY ([UserID]) REFERENCES [dbo].[Users] ([UserID])
);
───────────────────────────────────────────────────────────────



───────────────────────────────────────────────────────────────
4️⃣ BUDGET TABLE
───────────────────────────────────────────────────────────────
Purpose: Stores each user’s monthly budget and links to categories.
───────────────────────────────────────────────────────────────

CREATE TABLE [dbo].[Budget] (
    [BudgetID]     INT IDENTITY (1, 1) NOT NULL,
    [UserID]       INT NOT NULL,
    [CategoryID]   INT NULL,
    [BudgetAmount] DECIMAL (18, 2) NOT NULL,
    [Month]        INT NOT NULL,
    [Year]         INT NOT NULL,
    [CreatedDate]  DATETIME DEFAULT (GETDATE()) NOT NULL,
    PRIMARY KEY CLUSTERED ([BudgetID] ASC),
    FOREIGN KEY ([UserID]) REFERENCES [dbo].[Users] ([UserID]) ON DELETE CASCADE,
    FOREIGN KEY ([CategoryID]) REFERENCES [dbo].[Categories] ([CategoryID]) ON DELETE CASCADE,
    CONSTRAINT [CHK_Budget_Month] CHECK ([Month] >= 1 AND [Month] <= 12),
    CONSTRAINT [CHK_Budget_Year] CHECK ([Year] >= 2000 AND [Year] <= 2100),
    CONSTRAINT [CHK_Budget_Amount] CHECK ([BudgetAmount] > 0)
);

-- Indexes for faster search performance
CREATE NONCLUSTERED INDEX [IX_Budget_UserMonth]
    ON [dbo].[Budget]([UserID], [Month], [Year]);

CREATE NONCLUSTERED INDEX [IX_Budget_Category]
    ON [dbo].[Budget]([CategoryID]);
───────────────────────────────────────────────────────────────



───────────────────────────────────────────────────────────────
📊 RELATIONSHIPS
───────────────────────────────────────────────────────────────
Users (1) ────< Categories (many)
Users (1) ────< Income (many)
Users (1) ────< Budget (many)
Categories (1) ────< Income (many)
Categories (1) ────< Budget (many)
───────────────────────────────────────────────────────────────



───────────────────────────────────────────────────────────────
🧠 USAGE SUMMARY
───────────────────────────────────────────────────────────────
• All queries use parameterized SQL commands via ADO.NET
  (SqlCommand, SqlDataReader, SqlDataAdapter).

• Data Flow:
  - Users → Login/Register authentication
  - Categories → Expense CRUD operations
  - Income → Tracks earnings
  - Budget → Compares spending vs. planned limits

• Database supports ML.NET integration for:
  - Spending forecasts
  - Category clustering
  - Overspending prediction
───────────────────────────────────────────────────────────────
